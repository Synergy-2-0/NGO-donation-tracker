name: Production — Deploy on main

on:
  push:
    branches: [main]
    paths: ["backend/**"]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Production deploy (placeholder)
        run: |
          echo "Production deployment steps go here (e.g. build, docker push, run infra deploys)."
        working-directory: ./backend
